[supervisord]
nodaemon=true

[program:api]
command=uvicorn app.server:app --host 0.0.0.0 --port 8000
autorestart=true

[program:worker]
command=celery -A app.worker.tasks.app worker --concurrency=2
autorestart=true
